{
    "configurations": [
        {
            "name": "Wrangler: Attach",
            "port": 8787,
            "request": "attach",
            "skipFiles": ["<node_internals>/**"],
            "type": "node",
            "cwd": "${workspaceFolder}"
        }
    ]
}
